import { resetPasswordAction } from "../actions";
import { AuthForm } from "@/components/auth/auth-form";
export default async function ResetPasswordPage({searchParams}:{searchParams:Promise<{token?:string}>}){const{token}=await searchParams;return <div className="w-full max-w-md"><p className="eyebrow">Sécurité du compte</p><h1 className="serif mt-3 text-5xl">Nouveau mot de passe</h1>{token?<AuthForm action={resetPasswordAction} submitLabel="Modifier le mot de passe" fields={[{name:"token",label:"Jeton",type:"hidden"},{name:"password",label:"Nouveau mot de passe",type:"password",autoComplete:"new-password"},{name:"passwordConfirmation",label:"Confirmer",type:"password",autoComplete:"new-password"}]} initialValues={{token}}/>:<p className="mt-5 text-red-700">Lien incomplet.</p>}</div>}
