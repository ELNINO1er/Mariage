import { AuthForm } from "@/components/auth/auth-form";
import { loginAction } from "../actions";
export default async function LoginPage({searchParams}:{searchParams:Promise<{message?:string;error?:string}>}){const params=await searchParams;return <div className="w-full max-w-md"><a href="/" className="serif text-3xl lg:hidden">Noces.</a><p className="eyebrow mt-10 lg:mt-0">Heureux de vous revoir</p><h1 className="serif mt-3 text-5xl">Connexion</h1><p className="mt-3 text-sm text-[var(--caramel)]">Retrouvez votre mariage et vos invités.</p>{params.message&&<p className="mt-5 bg-emerald-50 p-3 text-sm text-emerald-800">{params.message}</p>}{params.error&&<p className="mt-5 bg-red-50 p-3 text-sm text-red-700">Votre compte ne peut pas se connecter.</p>}<AuthForm action={loginAction} submitLabel="Se connecter" fields={[{name:"email",label:"Adresse e-mail",type:"email",autoComplete:"email"},{name:"password",label:"Mot de passe",type:"password",autoComplete:"current-password"}]}/><div className="mt-5 flex justify-between text-sm"><a className="underline" href="/forgot-password">Mot de passe oublié ?</a><a className="underline" href="/register">Créer un compte</a></div></div>}
