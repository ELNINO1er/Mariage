type Mail={to:string;subject:string;text:string;html?:string};
export async function sendEmail(mail:Mail){const apiKey=process.env.RESEND_API_KEY,from=process.env.EMAIL_FROM;if(!apiKey||!from)return{sent:false as const,error:"Service e-mail non configuré"};const response=await fetch("https://api.resend.com/emails",{method:"POST",headers:{Authorization:`Bearer ${apiKey}`,"Content-Type":"application/json"},body:JSON.stringify({from,to:[mail.to],subject:mail.subject,text:mail.text,html:mail.html})});if(!response.ok)return{sent:false as const,error:`Échec e-mail (${response.status})`};return{sent:true as const};}
