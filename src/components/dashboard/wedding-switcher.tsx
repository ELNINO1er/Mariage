"use client";
import { selectWeddingAction } from "@/app/dashboard/actions";
export function WeddingSwitcher({current,options}:{current:string;options:{id:string;label:string}[]}){if(options.length<2)return null;return <form action={selectWeddingAction} className="mt-4"><label className="text-[10px] uppercase tracking-[.18em] text-[var(--caramel)]">Changer de mariage<select name="weddingId" defaultValue={current} onChange={(event)=>event.currentTarget.form?.requestSubmit()} className="mt-2 block w-full border border-[var(--beige)] bg-white px-3 py-3 text-sm"><option value="" disabled>Sélectionner</option>{options.map(option=><option key={option.id} value={option.id}>{option.label}</option>)}</select></label><noscript><button className="button-secondary mt-2 w-full">Ouvrir</button></noscript></form>}
